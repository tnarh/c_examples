#include <stdio.h>

int main(void) {
  int n1 = 0; // Initializing a variable
  
  printf("Enter a number: ");
  scanf("%d", &n1); // "scanf" waits for user input, with "&n1" representing the variable the users input will be saved to
  
  return 0;
}
